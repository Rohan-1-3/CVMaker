import React, { Component } from 'react';

class Form extends Component {
    render() {
        return (
            <form>
                <input/>
                <button>Submit</button>
            </form>
        );
    }
}

export default Form;